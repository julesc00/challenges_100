"""
LIFO = Last In First Out
    1. push(element) adds an element on top.
    2. pop() picks and removes the top element.
    3. peek() takes a peek at the top element.
    4. is_empty() checks if the stack is empty.
"""

import Stack


def main():
    stack = Stack()

